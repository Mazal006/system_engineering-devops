#!/usr/bin/env bash
# Print numbers 1 to 20 with superstitions using while and case

num=1
while [ $num -le 20 ]; do
  case $num in
    4) echo "bad luck from China" ;;
    8) echo "bad luck from Japan" ;;
    17) echo "bad luck from Italy" ;;
    *) echo "$num" ;;
  esac
  num=$((num + 1))
done
